import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Настройки")
                .font(.title)
                .padding()

            Text("Управление пользователями, уведомлениями и правами доступа.")
        }
    }
}
