import SwiftUI

struct CameraView: View {
    var body: some View {
        VStack {
            Text("Камера")
                .font(.title)
                .padding()

            Text("Сюда подключаем анализатор объектов и съемку для отчета.")
        }
    }
}
