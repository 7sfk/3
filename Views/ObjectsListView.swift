import SwiftUI

struct ObjectsListView: View {
    var body: some View {
        VStack {
            Text("Список объектов")
                .font(.title)
                .padding()

            Text("Здесь будет список всех объектов и проектов для контроля.")
        }
    }
}
