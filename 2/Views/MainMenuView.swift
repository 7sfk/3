import SwiftUI

struct MainMenuView: View {
    var body: some View {
        VStack {
            Text("Главное меню")
                .font(.title)
                .padding()

            Text("Здесь будут кнопки перехода на Dashboard, Камеру, Инвентарь и т.д.")
        }
    }
}
