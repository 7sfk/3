import SwiftUI

struct ObjectDetailView: View {
    var body: some View {
        VStack {
            Text("Детали объекта")
                .font(.title)
                .padding()

            Text("Здесь будут графические чертежи, этапы работ и учет.")
        }
    }
}
